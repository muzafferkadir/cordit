# ===========================================
# EXTERNAL PORTS
# ===========================================
# Only frontend is exposed to outside
FRONTEND_EXTERNAL_PORT=3021

# MongoDB for external tools (optional)
MONGO_EXTERNAL_PORT=27017

# ===========================================
# BACKEND SETTINGS
# ===========================================
HOST=0.0.0.0
PORT=3000
ENVIRONMENT=development
ACCESS_TOKEN_SECRET=your-secret-key-change-this-in-production

# ===========================================
# ADMIN SETUP
# ===========================================
CREATE_ADMIN_INITIALLY=true
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password

# ===========================================
# INVITE CODE SETTINGS
# ===========================================
INVITE_CODE_EXPIRY_HOURS=1

# ===========================================
# LIVEKIT CONFIGURATION
# ===========================================
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# ===========================================
# DOCKER INTERNAL NETWORK (auto-configured)
# ===========================================
# These are set by docker-compose, no need to change:
# - DATABASE_URL = mongodb://mongo:27017/litecord
# - LIVEKIT_API_URL = http://livekit:7880
# - LIVEKIT_URL = ws://livekit:7880
# - BACKEND_INTERNAL_URL = http://backend:3000
